# Ignore node_modules (dependencies can be installed via npm install)
node_modules/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment files (keep secrets safe)
.env

# OS-specific files
.DS_Store
Thumbs.db

# Build cache or output (optional, keep dist if you want to host it on GitHub Pages)
dist/*.map
src/output.css